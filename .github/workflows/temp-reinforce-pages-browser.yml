name: Temporary Pages and Browser Reinforcement

on:
  push:
    branches:
      - "guide/reinforce-pages-browser-compat"

permissions:
  contents: write

jobs:
  reinforce:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Apply reinforcement patch
        run: python maintenance/scripts/temp-reinforce-pages-browser.py

      - name: Remove temporary patch assets
        run: |
          rm maintenance/scripts/temp-reinforce-pages-browser.py
          rm .github/workflows/temp-reinforce-pages-browser.yml

      - name: Validate final state
        run: |
          node tests/validate-guide.mjs
          git diff --check

      - name: Commit reinforced guide state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Reinforce Pages and browser compatibility decisions"
          git push origin HEAD:${GITHUB_REF_NAME}
