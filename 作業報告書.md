# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-15 Human Guide Post-P1 Package A

### 目的

P0 / P1完了後の再評価で確認した2件のConcrete Coverage Defectを閉じ、同種のdriftをCurrent Registryから動的に検出できるようにした。

1. `site/pages/all-rules.html`がCurrent Owner 24件のうち`docs/17-visual-quality-baseline.md`を直接投影していなかった
2. Human Guide Manifestで`searchable: true`の`guide-migration`がSite-wide Search Registryから欠落していた

Point-in-time Audit本体は`EliteMay/web-project-data/evidence/2026/web-project-guide/audits/2026-09-15-human-guide-post-p1-reevaluation.md`へ保存済み。

### 実装

- `全ルール短縮版`のVisual cardからOwner 17 Visual Quality BaselineとOwner 18 Visual Researchの両方へ到達できるよう修正
- `site/data/search-sources.json`へGuide Migration Dashboardを`authority: status` / `contentType: human-page`として追加
- Site-wide SearchのAuthority filter / result labelへ`Status / 状態`を追加
- `tests/validate-human-guide.mjs`が`maintenance/rule-router.json`のCurrent Owner Registryを動的に読み、全Owner pathが`all-rules`へ投影されていることを検証するよう変更
- `tests/validate-site-search-coverage.mjs`が`authority`種類に依存せず、Manifestの全`searchable: true` SurfaceについてSearch Registry coverage / authority / destinationを検証するよう変更
- `UNRELEASED.md`へCoverage drift修正を記録
- Public URL、Owner Rule本文、GitHub Pages static deployment方式は変更していない

### Root Cause / Recurrence Guard

Human-facing projectionとSearch RegistryがCurrent Owner / Manifest contractから独立して管理され、一部Validatorも代表subsetまたは`human-summary`だけを確認していたため、Registryの一部欠落をCIが見逃していた。

症状だけをHardcodeで追加して終えず、次の動的Guardへ変更した。

- All Rules coverage → Current Machine Routerの`owners`から導出
- Search coverage → Human Guide Manifestの`searchable: true`から導出

既存Learning `PL-F-018 Human Guide Shell Refactorで既存機能群を落とした`と同じ「Presentation変更時のCoverage preservation」系列であり、新しいCommon Owner /重複Learningは追加していない。

### GitHub Evidence

- Implementation PR: `EliteMay/web-project-guide#116`
- PR final head: `83e01e56adb90f37e55f042b890cd31e02f987f5`
- Squash merge commit: `cf4f7777d84e0691fc8edd02be7abfa7c45d86fd`
- PR-head `Validate Guide #420`: **PASS**
- Post-merge `Validate Guide #421`: **PASS**
- GitHub Pages #261: **PASS**
- Public URL: `https://elitemay.github.io/web-project-guide/`

### Browser Verification

GitHub Pages #261が生成した本番配信用Artifactを展開し、そのHTML / Search Registry / Search RuntimeをChromium test harnessで確認した。実行環境Policyで通常URL navigationがblockされるため、Artifact内容を変更せずBrowser DOMへ読み込み、SearchのURL base / fetchだけをtest harness側で置換した。

確認結果:

- `全ルール短縮版`に`17 Visual Quality Baseline` link: **1件**
- 同Visual cardに`18 Visual Research` link: **1件**
- Search Authority option `status`: **存在**
- `migration` + `authority=status`: **1件表示**
- Result: `Guide Migration Dashboard`
- Authority badge: `Status / 状態`
- Search runtime JavaScript error: **なし**

### Current Status

**作業状況: 完了**

Package Aの既知Defectは解消し、Current mainのStatic Validation / Pages deployment /配信Artifact Browser checkまで確認済み。次の候補はPost-P1 AuditのPackage B（Human Guide ManifestとSearch metadataの重複削減、複数語Search quality改善）であり、Package Aのblocking issueは残っていない。
