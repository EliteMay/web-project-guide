# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-14 Human Guide P1改善

### 目的

P0で整えたManifest / Global Navigation / Site-wide Search / Task Router基盤の上に、長いPage内の探索、Source / 修正導線、Searchの絞り込みとKeyboard操作を追加し、Rule本文や第二Source of Truthを増やさずHuman GuideのFindabilityを補強した。

### 実装

- `HUMAN_GUIDE_REQUIREMENTS.md`をP0実装待ち文書からCurrent Product Contractへ整理
- `site/data/human-guide-manifest.json`へAuto TOC opt-in metadataを追加
  - `ai-workflow`
  - `all-rules`
  - `research-requirements`
  の3Surfaceだけを対象化
- `site/assets/human-guide-shell.js`へManifest-driven Auto TOCを追加
  - `main`内の`h2` / `h3`から自動生成
  - Heading IDが無い場合は重複しないAnchorを付与
  - Current sectionへ`aria-current="location"`
  - Desktopではsticky、Mobileでは通常Flowへ戻す
- Manifestの`sourceLinks[]`から共通`Source / 修正` Footerを生成
  - Source / Owner / Requirement
  - GitHub Edit
  - Issue Report
- `site/assets/human-guide-components.css`を追加し、TOC / Source Footerを既存Design Systemに合わせて共通化
- `site/data/search-sources.json`へ`contentType` metadataを追加
- Site-wide Searchを次へ拡張
  - Authority Filter
  - Content Type Filter
  - Search resultでAuthority / Content Typeを別表示
  - `q` / `authority` / `type`をURLへ保存しReload時に復元
  - `/`でSearch focus
  - `Ctrl/Cmd + K`でSearch focus
  - Input等へ入力中の`/`は奪わない
- `tests/validate-human-guide.mjs`と`tests/validate-site-search-coverage.mjs`を拡張
- P0で追加したRule Finder 19機能Coverage Guardを維持
- `UNRELEASED.md`へP1差分を記録

### GitHub / Validation Evidence

- PR: `EliteMay/web-project-guide#112`
- PR final head: `27ac85cae55acd3ca28c10dfc952e89798262814`
- PR-head `Validate Guide` run `#411` (`34840533566`): **PASS**
- PR #112: **merged**
- Squash merge commit: `9107436b1792601f80fd3ab6c4893e3379d3e007`
- Post-merge `Validate Guide` run `#412` (`34840683661`): **PASS**
- GitHub Pages run `#258` (`34840682852`): build / deploy **PASS**
- Pages artifact ID: `10346360587`
- Pages artifact digest: `sha256:a54f0e478a02b7d8d48b4aa846d404cb4929aaee0183253bdfc64bff36a82cd6`
- Public URL: `https://elitemay.github.io/web-project-guide/`

### Browser Verification

GitHub Pages run #258が生成した**実build artifact**をChromiumでHTTP配信し、Desktop / 390px narrow viewportで確認した。

最終結果: **61 checks PASS**

主な確認内容:

- Auto TOC対象Page
  - AIの作業ルール: 20 links
  - 全ルール: 27 links
  - 研究・要件: 41 links
- Rule Finder / Search / Task RouterにはTOCを自動追加していない
- TOC Anchorが実Headingへ移動できる
- Current section stateを付与できる
- Source FooterからSource / Edit / Reportへ到達できる
- `保存`検索で30件取得
- Search resultがAuthority / Content Typeを両方表示
- `Owner / 正本 + Owner Doc` Filterで19件取得
- `q` / `authority` / `type`をReload後も復元
- `/`でSearchへfocus
- Input入力中の`/`は通常文字として入力可能
- `Ctrl+K`でSearchへfocus
- 390px viewportでTOCは初期collapsed / `position: static`
- 390pxでSearch controlsは1列
- Global Navigationは6 linksを維持しhorizontal scroll可能
- Body horizontal overflowなし
- Test flow中のHTTP 4xx / 5xx responseなし
- Desktop TOC / Mobile Search / Source FooterをScreenshotでも確認

Browser確認中、数字から始まるHeading IDをテスト側がCSS selectorとして直接扱ったため一度Test Harnessが停止したが、Product側のAnchor動作ではなく検証コード側の問題だった。`getElementById`基準へ修正して再実行し、Product Bugは確認されなかった。

### Current Status

**作業状況: 完了**

Human Guide P1のRuntime変更はmainへMerge済みで、PR-head CI、post-merge main CI、GitHub Pages deployment、実Pages artifactのBrowser / responsive / keyboard確認まで成功している。Current known blocking issueはない。
