# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-11 Release / Current State Integrity

### 作業状況

Guide 1.22.0 Release後もNormative Rule / Human Guide / Validator等が更新されていた一方、`guide-version.json`と`CHANGELOG.md`が1.22.0のRelease状態だけを示し続けても既存CIが成功するDriftを確認した。

症状だけのVersion bumpは行わず、最後にReleaseしたBaselineとCurrent mainの未Release変更を分離する仕組みへ修正した。

### Root Cause

既存Validatorは次だけを比較していた。

```text
guide-version.json.guideVersion / updated
↕
CHANGELOG latest release heading
```

両方が同時に古い場合、Current RepositoryがRelease baselineより進んでいても不一致を検出できなかった。つまりVersion値の更新漏れではなく、**Release boundaryとCurrent Repository差分をValidationしていないこと**が原因だった。

### 実装

- `guide-version.json`
  - `1.22.0 / 2026-09-07`を最後のReleased baselineとして維持
  - 実際に1.22.0をReleaseしたPR #65のmerge commit `fd9cc77ff5284906784ca827d4c21caa2950d08d`を`releaseCommit`として記録
  - Current mainにRelease後変更があるため`status: unreleased-changes`へ変更
  - `unreleasedLog: UNRELEASED.md`を追加
- `CHANGELOG.md`
  - Release済み履歴専用として既存内容を完全保持
- `UNRELEASED.md`
  - 1.22.0以降のCurrent未Release差分を整理
- `docs/09-maintenance.md`
  - Released baseline / Unreleased current stateの分離Contractを追加
- `tests/validate-release-integrity.mjs`
  - releaseCommit ancestry / baseline metadata
  - release後のRelease-affecting diff
  - status / Unreleased ledger整合
  - PR / push単位でRelease-affecting変更時に`UNRELEASED.md`が同時更新されたこと
  を検証
- `.github/workflows/validate-guide.yml`
  - Full git historyを取得
  - Release integrity validatorをCIへ追加
- `README.md`
  - Version番号だけでなく`status`とCurrent Repositoryを確認する境界を明記

### 実装中に見つかった追加Failure

一度`CHANGELOG.md`へUnreleasedを直接追加した際、full-file replacementで過去Release詳細を一部要約してしまった。PR作成前に検出し、旧Blobをそのまま復元した。

このためCurrent差分は巨大なRelease historyを書き換えず、専用`UNRELEASED.md`へ分離した。Release済み履歴をCurrent trackingのために毎回再構築しない。

また最初に`releaseCommit`として40-point closure commit `7c2a124...`を選んだが、そのCommitの`guide-version.json`はまだ1.21.0だった。新Validator自身がこれを検出し、実Release commit `fd9cc77...`へ修正した。

### Validation

- Branch: `fix/release-current-state-integrity`
- Base: `86aab472830d6551e219ecc4c1fb4c97eeda2d64`
- PR: #91
- PR-head `Validate Guide` run `34556946265`: success
- merge commit: `02bd079b8c7507641fe1b76e1bd9f24efd39c771`
- merged-main `Validate Guide` run `34556975426`: success
- merged-main Pages build / deployment run `34556974962`: success
- Product Runtime / Save / Schema変更: なし

### Remaining

- `guideVersion`は新Releaseを切っていないため`1.22.0`のまま。`status: unreleased-changes`がCurrent mainとの差分を明示する。
- 次回正式Release時に`UNRELEASED.md`の内容を`CHANGELOG.md`の新Version entryへ移し、release baselineを更新する。
- P0の次項目であるHuman / Machine Guide Parity、Dashboard V2等は別作業として扱う。
