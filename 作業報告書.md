# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-11 Release / Current State Integrity

### 作業状況

Guide 1.22.0 Release後もNormative Rule / Human Guide / Validator等が更新されていた一方、`guide-version.json`と`CHANGELOG.md`が1.22.0のRelease状態だけを示し続けても既存CIが成功するDriftを確認した。

症状だけのVersion bumpは行わず、最後にReleaseしたBaselineとCurrent mainの未Release変更を分離する仕組みへ修正した。

### Root Cause

既存Validatorは次だけを比較していた。

```text
guide-version.json.guideVersion / updated
↕
CHANGELOG latest release heading
```

両方が同時に古い場合、Current RepositoryがRelease baselineより進んでいても不一致を検出できなかった。つまりVersion値の更新漏れではなく、**Release boundaryとCurrent Repository差分をValidationしていないこと**が原因だった。

### 実装

- `guide-version.json`
  - `1.22.0 / 2026-09-07`を最後のReleased baselineとして維持
  - `releaseCommit`を追加
  - Current mainにRelease後変更があるため`status: unreleased-changes`へ変更
- `CHANGELOG.md`
  - `Unreleased`を追加し、1.22.0以降のMeaningful changeを整理
- `docs/09-maintenance.md`
  - Released baseline / Unreleased current stateの分離Contractを追加
- `tests/validate-release-integrity.mjs`
  - releaseCommit ancestry
  - release後のRelease-affecting diff
  - status / Unreleased整合
  - PR / push単位でRelease-affecting変更時にCHANGELOGが同時更新されたこと
  を検証
- `.github/workflows/validate-guide.yml`
  - Full git historyを取得
  - Release integrity validatorをCIへ追加
- `README.md`
  - Version番号だけでなく`status`とCurrent Repositoryを確認する境界を明記

### Validation

- Branch: `fix/release-current-state-integrity`
- Base: `86aab472830d6551e219ecc4c1fb4c97eeda2d64`
- PR-head `Validate Guide`: PR作成後に確認
- merge後main `Validate Guide`: Merge後に確認
- Product Runtime / Save / Schema変更: なし

### Remaining

- `guideVersion`は新Releaseを切っていないため`1.22.0`のまま。`status: unreleased-changes`がCurrent mainとの差分を明示する。
- 次回正式Release時にUnreleasedをRelease entryへ移し、release baselineを更新する。
- P0の次項目であるHuman / Machine Guide Parity、Dashboard V2等は別作業として扱う。
