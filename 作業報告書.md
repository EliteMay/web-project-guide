# 作業報告書

直近の作業内容・確認状態・未確認事項を記録します。過去の詳細はGit history、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`を参照します。

## 2026-09-17 検証漏れの修復

### 対象とPreflight

- 対象: `EliteMay/web-project-guide`、開始Commit `a8e78845712fc9607be29127ad17499a4d866cae`。
- README / START_HERE / Current Requirements / Router、およびGovernance・Testing・Maintenance・Project Management・Continuous Improvement・Loop Contractを確認。
- Known Failure: PL-F-002 / PL-F-018と関連Catalogを確認。正常系だけでなく不正入力を使って検出能力を検証した。
- 方式: 発見 → 故障注入による再現 → 最小修正 → Regression → 再検証。専用Loop Runtime ControllerによるProduction Pilotは実行していない。
- 未MergeのPhase E同期PR #124とは分離し、Runtime仕様・権限Schema・公開UIは変更しない。

### 再現と修正

1. RootのLoop要件書に存在しないFileへのリンクを追加してもGuide Validatorが終了コード0を返した。固定Root一覧を全Root Markdownの動的列挙へ変更し、検査対象が106文書から114文書になった。
2. Safe Exampleの`pushWorkingBranch`をtrueにしてもLoop Validatorが終了コード0を返した。remote push禁止、isolated commit許可、検証Evidence必須を明示検査する。
3. リンクの不正なURLエンコードは未捕捉例外になっていた。診断を集約して、後続のリンク検査も続行する。

一時コピーで正常系・8種類のRoot文書・将来のRoot文書・H1・不正エンコード・危険または欠落した設定を検査する22チェックのRegressionを追加し、既存CIへ接続した。

### 検証

- `node tests/test-validator-failure-detection.mjs`: 22 checks PASS。
- Guide / Loop Contractを含む、Git履歴を必要としない15検証プログラムが全てPASS。
- `validate-release-integrity.mjs`は完全なGit履歴が必要。この環境ではGitHub APIから固定CommitのFileを取得しているため、履歴依存の検証はPRのGitHub Actionsで確認する。
- Browser / Windows: 今回はUI・Runtimeを変更していないため実行していない。

### 完了境界

修正候補はReview Branch / PRで保存する。mainへのMerge・公開反映はこの候補のCI確認とReview後の工程であり、ローカルPASSのみで反映済みとは扱わない。
