# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-14 Guide Scope / Routing Audit Remediation

### 目的

Guide全体へ新しいRuleを増やす前に、Current `main`を基準として次の構造問題を監査・修正した。

- Deep Audit結果の保存先がGuide / Dataで矛盾している
- Conversation PersistenceのCompletion責務が全Work TypeのPreflight Routeへ混ざっている
- `docs/23`がData repositoryのCurrent implementation detailまで複製している

一般Usability Testing、Browser Permission / Powerful Web API、`docs/04` / `docs/18` Visual Workflow重複、PWA Product Contractは別Findingとして残し、この構造修正PRへ混ぜていない。

### Audit Evidence

Point-in-time Audit本体:

- `EliteMay/web-project-data/evidence/2026/web-project-guide/audits/2026-09-14-guide-scope-routing-audit.md`

Guide側へAudit結果履歴を増やさず、`maintenance/audits/`はCompatibility Pointer / Indexのまま維持する。

### 実装

PR: `EliteMay/web-project-guide#107`

#### 1. Preflight / Interaction Lifecycle分離

- `maintenance/rule-router.json`から`docs/23-conversation-handoff-recovery.md`を全Work TypeのRequired Docsから除外
- `interactionLifecycle.completionDocs`を最小の独立Layerとして追加
- Conversation Recovery自体がTaskの場合は`CONVERSATION_HANDOFF` Domain / `CONVERSATION_STATE_RECOVERY` Signalから従来どおりPreflightで`docs/23`へ到達
- Local UI Bug Golden Caseで`docs/23`がPreflight必須へ戻らないことをnegative guard化
- Router Schema / general Validator / Persistence Validatorを同期

#### 2. Audit storage boundary修正

- `maintenance/DEEP_SYSTEM_AUDIT.md`をData側Audit保存先へ変更
- `maintenance/review-policy.json`へ`resultRepository = EliteMay/web-project-data`、Data側`resultDirectory`、Guide側`compatibilityIndex`を分離して記録
- General ValidatorでこのStorage metadataをRegression Guard化

#### 3. Guide / Data implementation boundary修正

`docs/23`は次のBehavioral Contractを維持:

- Guide対象InteractionのPersistence Completion
- Receiptなしで保存成功を推測しない
- Current RepositoryをProject StateのAuthorityとして維持
- Automatic Resume Confirmation / Write Target / Guard分離
- authoritative mutation前のGuard
- unsettled Guard recovery
- TTL-only takeover禁止
- privacy / degraded mode / platform boundary

一方、Current implementationの次はData repositoryへ委譲:

- Schema
- File path
- Branch / Ref名
- write algorithm
- Receipt field
- Settlement implementation
- current helper / tooling name

#### 4. Learning / Release note

- `PROJECT_LEARNINGS.md`へ`PL-F-016 Completion LifecycleをPreflight Routeへ混ぜた`を追加
- 旧`PL-S-005`のAudit保存先説明をCurrent Guide/Data boundaryへ更新
- `UNRELEASED.md`へLifecycle分離・Audit storage・Guide/Data境界修正を記録

### Validation

CIで実際に2種類のstale Validatorを検出した。

1. `validate-development-conversation-persistence.mjs`が旧Contractとして「全Work Typeへdocs23を直接入れる」「recovery-live等のCurrent implementation文字列をGuide本文へ置く」ことを固定していた。
2. `validate-root-cause-workflow.mjs`がResume rejectionの1つの旧表現だけを完全一致で要求していた。

Rule本文を旧構造へ戻さず、ValidatorをBehavior / Lifecycle構造へ合わせた。

確認済み:

- `tests/validate-guide.mjs`: pass
- release lifecycle / current-state integrity: pass
- audit contract: pass
- data storage contract: pass
- Human Guide validation: pass
- development conversation persistence validation: pass
- root-cause workflow validation: pass
- 機能変更を含むPR-head `Validate Guide` run `34765295700` / run number `377`: **success**

このWork Report / Learning等の最終bookkeeping commit後もCurrent PR headでCIを再確認し、その最終runをCompletion Evidenceとする。

### Repository Operation Note

Branch作成Capability確認中に、一時File `__probe__`を誤って`main`へ作成した。

- accidental create commit: `788316c55b6fedb55be006e88a5496227b9ccbae`
- immediate cleanup / restored main commit: `f1a4e0469375b9bf47088e6eba17e79fe1bad980`

Fileは即削除し、remediation branch `audit-routing-cleanup-20260914`は復旧後のmainから作成した。永続Content変更は残っていないが、2 Commitの履歴は残るため隠さず記録する。

### Remaining

今回のPR Scope外として次を未解決のままData側Auditへ記録している。

- Medium: `docs/04` / `docs/18` Visual Workflow重複
- Medium: General task-based Usability Testing
- Medium: Browser Permission / Powerful Web API lifecycle
- Medium / P2: PWA Product Contract
- Medium / Monitor: Web Product GuideとAI Development Operating Systemの長期Scope境界

これらを今回直した扱いにしない。
