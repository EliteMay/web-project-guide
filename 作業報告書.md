# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-24 Electron Common Foundation V2

### 目的

前回追加したElectron共通基盤を、長時間Task・外部Process・Secret・Download・Repair・Power lifecycle・App identityまで拡張し、複数Electron Appへ再利用できるDesktop Foundationとして補強した。

### 変更

- `docs/11-electron-distribution.md`へTask Manager Contractを追加し、queued / running / interrupted / failed / cancelled等の実State、Progress、Cancel、Retry、Resume、Restart recoveryを整理
- Process Manager / SupervisorとしてStart / Stop / Restart / PID / Health Check / stdout / stderr / orphan cleanup / bounded restart / Crash Loop検出を追加
- Crash Loop時のSafe Mode / Repair、破壊範囲別のCache Reset / Settings Reset / Backup Restoreを追加
- API Key / Token / Password等を通常設定から分離するSecret Storage Contractを追加
- 大容量DownloadのPause / Resume / Cancel / interruption / Integrity / temporary file / Disk full対応を追加
- Diagnostic Export、Disk / Cache lifecycle、Suspend / Resume等のPower lifecycle、First Run / Repair flowを追加
- 独立配布するElectron AppはElectron既定Iconや他Appと同一Iconのままにせず、Appごとに識別可能な固有Iconを持つRuleを追加
- Packaged executable / BrowserWindow / Taskbar / Desktop Shortcut / Start Menu / Installer / Uninstaller等の必要SurfaceでIconを揃えるContractを追加
- Icon変更時も`productName` / App ID / AppUserModelID等のstable identityを理由なく変更せずCompatibilityを確認するRuleを追加
- `templates/QUALITY_CHECKLIST.md`へ上記のVerificationを追加
- `UNRELEASED.md`へ未Release変更を記録

### External Evidence確認

Current Electron公式Documentationで次のCapabilityを確認した。

- `utilityProcess`: Node.js child processをChromium Services API経由で起動可能
- `safeStorage`: OS提供の暗号化機構を利用したlocal secret protection
- `DownloadItem`: Download progress / cancel / resume availability等を扱える
- `powerMonitor`: suspend / resume / AC / battery等のpower stateを監視可能
- Windows Taskbar / Packaging Documentation: Taskbar customizationとWindows executable icon rebrandingをサポート

Rule本文ではこれらのAPIを全Appへ固定採用せず、Project要件に応じたCapability Contractとして定義した。

### Rule Budget / Routing

- Electronの既存Normative Owner `docs/11`へ統合
- 新しいOwner / Gate / Routerは追加していない
- Secretの一般Securityは`docs/06`、Data lifecycleは`docs/03`、Diagnosticsは`docs/15`の正本を維持
- Electron固有のOS lifecycle / Process / Task / packaging identityだけを`docs/11`で定義

### Validation

- Guide Validator / Link / Router整合をPR CIで確認する
- OS / Shell / Icon behaviorはStatic / CIだけでReal-device validated扱いにしない
- Product Repositoryの既存Electron Appへ自動で機能追加・Icon変更はしていない

### Current Status

**作業状況: Rule本文 / Checklist / Unreleased / Work ReportをBranchへ反映済み。PR CIとmerge後main Validationで完了判定する。**

## 2026-09-23 Electron Common Foundation Rule

### 目的

Auto Updateのように複数Electron Appで繰り返し必要になるDesktop共通責務を、App固有機能から分離して再利用できるRuleとして整理した。

### 変更

- `docs/11-electron-distribution.md`へ`Electron共通基盤` Sectionを追加
- Settings / Log / Diagnostics / Window State / Single Instance / Recovery /最近使ったPath / Theme / Network stateを基礎Capability候補として整理
- 重要Local DataのBackup / Restore / Last-known-good recoveryをData Ruleへ接続
- Auto Start / Tray / Notification / Global Shortcut / File Association / GitHub Integration / Cache Clear等は全Appへ強制せずConditional Capabilityへ分離
- 複数Appで共通化する場合はTemplateまたは内部Packageを使えるが、App固有Business Logicを取り込まずFeature単位でopt-inできるContractを追加
- `templates/QUALITY_CHECKLIST.md`のElectron SectionへRestart persistence、off-screen Window prevention、Single Instance、Backup / Recovery、Diagnostics sanitization、shared foundation regression確認を追加
- `UNRELEASED.md`へ未Release変更を記録

### External Evidence確認

Current Electron公式Documentationで、次のPlatform capabilityが現行APIとして提供されていることを確認した。

- `app.requestSingleInstanceLock()` / `second-instance`
- `app.getPath('userData')` / `app.setAppLogsPath()`
- `nativeTheme`
- `Notification`
- `crashReporter`

Rule本文ではAPI名を固定実装Requirementにせず、採用時にCurrent Electron公式仕様を再確認するContractとした。

### Rule Budget / Routing

- Electronの既存Normative Ownerである`docs/11`へ統合し、新しいOwner / Gate / Domainは追加していない
- `README.md` / `START_HERE.md` / `maintenance/rule-router.json`のElectron Routeは既に`docs/11`へ到達するため変更不要
- Data安全は`docs/03`、Themeは`docs/04`、Diagnostics / Observabilityは`docs/15`へOwnerを維持し、`docs/11`ではElectron固有の適用境界だけを定義した

### Validation

- Markdown / Link / Router整合はGuide Validatorで確認する
- OS統合機能はStatic確認だけでReal-device validated扱いにしないRuleを維持
- Guide側Rule変更のためProduct RepositoryのElectron実装は自動変更していない

### Current Status

**作業状況: PR #128をmerge済み。PR headとmerge後mainのValidate GuideはいずれもPASS。Electron共通基盤V1はmainへ反映済み。**

## 2026-09-19 Dark / Night Mode Default Rule

### 目的

Project Ownerが普段Dark / Night Modeを利用し、明るい大面積Surfaceを眩しく感じやすいという継続的な利用条件を、複数Projectで再利用できるUI Defaultとして整理した。

### 変更

- `docs/04-ui-ux-accessibility.md`へTheme / Brightness Ruleを追加
- Owner本人向けUIでは、明示的な別要件がなければDark Themeを初期Themeの標準候補とする
- 一般公開ProductではTarget User / Domain / Brand / Accessibility要件を優先し、Owner個人の好みだけで強制しない
- Theme切替時の選択保存、`prefers-color-scheme`利用、Dark Theme上のContrast確認、初回描画のWhite Flash回避を明文化

### Validation

- 既存Owner `docs/04`へ統合し、新しいOwner / Gate / Routerは追加していない
- `UNRELEASED.md`へ未Release変更として記録
- Guide Validator / PR CIはPR作成後に確認する

## 2026-09-17 Loop Engineering Runtime Phase D

### 目的

Phase AのRead-only / Dry Run、Phase BのIsolated Worker Loop、Phase CのResume / Stuck / Budget / Controlに続き、**明示的に独立と確認できる複数Taskを安全に並列実行するPhase D Parallel Worker Orchestration**を実装した。

Phase Dの目的はWorker数を増やすこと自体ではない。Phase Cまでの`L1_WORKTREE`安全境界を維持したまま、依存・Path scope・Semantic scopeが独立したTaskだけを並列化し、cross-worker Integration VerificationがPASSするまでQueue completionしないこと。

### Runtime実装

実装先: `EliteMay/web-project-data`

主な追加:

- `tools/loop-engineering/phase-d-controller.mjs`
- `tools/loop-engineering/test-phase-d-controller.mjs`
- `schemas/loop-engineering/phase-d-plan.schema.json`
- `schemas/loop-engineering/phase-d-state.schema.json`
- `schemas/loop-engineering/phase-d-receipt.schema.json`
- `Validate Loop Engineering`へPhase D Ubuntu / Windows jobs
- Data README / Loop Runtime README / Loop Runs README
- point-in-time Evidence
  - `evidence/2026/web-project-guide/loop-engineering-phase-d-runtime-evidence.md`

### Parallel Eligibility

Phase Dは`safeParallel=true`だけでは開始しない。

開始条件:

- selected Taskが全て`safeParallel=true`
- selected Task間にdependencyがない
- 外部dependencyがresolved
- Current Requirements revision一致
- 各Laneが利用可能
- `scopePaths[]`がsafeなliteral relative prefix
- Path scopeが相互に重ならない
- `semanticScopes[]`が相互に重ならない
- Policy `allowedPaths`内で、`protectedPaths`と重ならない
- selected Task以外のactive write authorityがない
- `maxParallelWorkers` / Budget内

不明確なTaskを「たぶん独立」と推測して並列化しない。

### Execution Flow

```text
Current Repository / Queue / Requirements
↓
parallel eligibility check
↓
atomic multi-task assignment
↓
formal claim × N
↓
dedicated worker branch / isolated worktree × N
↓
parallel implementation
↓
protected worker verification × N
↓
deterministic Integration Branch
↓
candidate commitsをtaskId順にapply
↓
cross-worker Integration Verification
↓
all PASS -> Queue completion × N
```

Repository-wide writer lockはPhase D Controllerが1つ保持し、外部Writerとの競合を防ぐ。Worker同士は別Branch / worktreeで実装する。

Candidate CommitはTask IDのdeterministic orderで専用Integration Branchへ適用する。cherry-pick conflictは自動解決せずfail closedする。

### Verification / Completion Gate

WorkerごとにTask-specific validation requirements + Policy required checksを満たすprotected verificationを要求する。

Worker全件がPASSしても、Integration Branch上のcross-worker Integration VerificationがPASSするまではQueueを`completed`へ進めない。

Integration Verifier failure / conflict / Budget exhaustion / Cancelではowned Taskをformal Queue transitionで停止し、成功へ丸めない。

### Budget / Kill Switch

Phase Dは全Worker + Integrationをまとめて次のAggregate Budgetで評価する。

- wall-clock
- model tokens
- external cost
- parallel worker count

`cancel`はGlobal Kill Switchとして扱う。

### Safety Boundary

Phase Dでも`L1_WORKTREE`境界を維持する。

- Target Default Branch direct write禁止
- Default Branch SHAを開始時から変更しない
- Worker / Integration Branchはlocal
- Push禁止
- RuntimeによるPR作成なし
- Merge禁止
- Deploy禁止
- Secret capabilityなし
- WorkerによるVerifier改変禁止
- OS-level sandbox / network namespaceはRuntime内蔵と扱わない

### Regression Scenarios

Phase D Integration Testで確認:

1. 2 Worker implementation callbacksが実際に同時進行する
2. 独立Path / Semantic scopeを受理する
3. overlapはQueue mutation前に拒否する
4. Workerごとのprotected verificationを通す
5. Candidate Commitをdeterministic orderでIntegration Branchへ統合する
6. Integration Verifier PASS後だけ全Taskをcompletedへ進める
7. Integration failureではTaskをcompletedにしない
8. Aggregate token budget exhaustionを`budget_exhausted`として停止する
9. Target primary `main` SHA不変
10. Ubuntu / Windows双方で同じContractが成立する

### Validation中に検出した問題

最初のPhase D CIでは、overlap test fixtureがPolicyの`allowedPaths: src/**`に対して`src`を指定しており、overlap判定より先にPolicy外scopeとして正しく拒否された。Test inputを有効scope同士が重なるケースへ修正した。

その後、Phase D test fileがbranch update途中で一時的に欠落する状態を検出し復元した。

Cross-platform validationではWindowsのGit checkoutがCRLFをmaterializeし得るため、fixtureのraw改行完全一致がUbuntuと異なる結果を返した。Runtime Safety Boundaryを緩めず、Testをsemantic content比較へ変更した。

### Verified CI Evidence

Phase D implementation head:

`a316c64dd291978683b04861b3318250f76b21d5`

PASS:

- `Validate Loop Engineering #28`
  - Phase A
  - Phase B Ubuntu / Windows
  - Phase C Ubuntu / Windows
  - Phase D Ubuntu / Windows
- `Validate Data #442`
- `Validate Reliability Contracts #171`
- `Validate Windows PowerShell Compatibility #231`

### GitHub Evidence

Data Runtime:

- Pull Request: `EliteMay/web-project-data#152`
- Squash merge commit: `2913e1b02f2fcd1392fc350e2ca911a562d9a57c`

Point-in-time Evidence:

- `EliteMay/web-project-data/evidence/2026/web-project-guide/loop-engineering-phase-d-runtime-evidence.md`

PR終盤のDocumentation / Evidence-only headではGitHub-hosted runnerが`runner_id: 0` / `steps: []`のまま割り当てられず、Loop / Data / Reliability / Windows workflowがCode実行前に失敗した。失敗Jobは全てRunner未割当でありRuntime Test failureとは区別した。直前の実装headでは上記Required CIが全PASSしている。

### 未確認 / 未実装

Phase D Validationはtemporary fixture Repository / Queueを使ったIntegration Testである。

**実ProjectのCurrent Queue TaskをPhase C / Dで長時間自律実行するProduction Pilotは`NOT_RUN`。**

未実装:

- Phase E Remote Publication / Review Integration
- automatic branch push
- RuntimeによるPR creation
- guarded Default Branch Merge
- Release / Production Deploy automation
- OS-level sandbox / network namespace
- verifier用別Credential processのRuntime内蔵

### Current Status

**作業状況: Guide-side Foundation + Runtime Phase A + Runtime Phase B + Runtime Phase C + Runtime Phase D実装済み / Phase E未実装 / Real-project Production Pilot NOT_RUN**

Guide側は`LOOP_ENGINEERING_REQUIREMENTS.md`とfocused validatorをPhase D実装状態へ同期し、Guide CI / Pagesを確認して完了判定する。
