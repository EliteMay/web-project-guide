# 作業報告書

このファイルは**直近の作業内容・確認状態・未確認事項**だけを記録します。

長期変更履歴は`CHANGELOG.md`、未Release差分は`UNRELEASED.md`、再発防止知識は`PROJECT_LEARNINGS.md`、詳細差分はGit history / PRを正本とします。

## 2026-09-14 Human Guide P1 Related / Freshness改善

### 目的

Human Guide P1のうち既にmainへ入っていたAuto TOC、Source / Edit / Report Footer、Search Filter / Shortcutを重複実装せず、未実装だった次の2領域を補完した。

1. Pageを読み終えたあとに次の適切なHuman Guide Surfaceへ進めるRelated / Next Steps導線
2. Guide Release Baseline表示とHuman Summary本文の同期保証を混同しないFreshness表示

### 実装

- `site/data/human-guide-manifest.json`へ`relatedSurfaceIds[]`を追加
- 共通ShellがManifestから「次に見る」Sectionを生成するよう変更
- Related先は既知canonical Human Guide surfaceだけに限定
- 自己参照 / 重複 / 未知IDを専用Validatorで禁止
- Relatedが空のSurfaceにはSectionを無理に表示しない
- AI Recommendation Engineや動的推測は導入せず、明示metadataだけを使用
- Source FooterへRelease / Freshness説明を追加
- Human Summaryでは「Release表示は本文同期保証ではない」と明示し、判断時はSourceへ戻れる状態にした
- Narrow viewportではRelated / Source Footer linkを1列へStack
- `HUMAN_GUIDE_REQUIREMENTS.md`へRelated / Next StepsとFreshnessのCurrent Contractを統合
- `tests/validate-human-guide-related-freshness.mjs`を追加し、Manifest / Shell / CSS / Requirementsを横断検証
- `Validate Guide` workflowへ専用Regression Guardを追加
- `UNRELEASED.md`へP1後半の変更を記録

### Validation / Failure Review

PR #114では2回CIが停止したが、どちらもMerge前に原因を確定して修正した。

1. `Validate Guide #414` — **FAIL**
   - 原因: Release-affecting変更に対して`UNRELEASED.md`を同じPRで更新していなかった
   - 対応: Release ledgerへAdded / Changedを追加
   - Existing release-integrity guardが意図どおり機能した
2. `Validate Guide #415` — **FAIL**
   - 原因: 新規ValidatorがRuntimeの`dataset.humanGuideRelated` / `dataset.humanGuideFreshness`を、誤って`data.humanGuideRelated` / `data.humanGuideFreshness`として検査していた
   - 対応: Validator markerを実装表現へ一致させた
3. `Validate Guide #416` — **PASS**
4. PR #114をsquash merge
5. Merge後 `Validate Guide #417` — **PASS**
6. GitHub Pages #259 — build **PASS** / deploy **PASS**

### GitHub Evidence

- PR: `EliteMay/web-project-guide#114`
- PR final head: `161553253544d29443772133e4b0df058e00386a`
- Squash merge commit: `65febce426f34d77ee6dc8dac5d8b693cb919f68`
- PR-head `Validate Guide #416`: **PASS**
- Post-merge `Validate Guide #417`: **PASS**
- GitHub Pages #259: **PASS**
- Public URL: `https://elitemay.github.io/web-project-guide/`

### Browser Verification

GitHub Pages #259が生成した本番配信用ArtifactのHTML / JavaScript / CSS / Manifest / `guide-version.json`をChromium test harnessへ読み込み、Navigation制限がある実行環境のためURL baseだけをtest URLへ置換してRuntime behaviorを確認した。

Rule Finderで確認した結果:

- Related Section: 1件生成
- Related Links: `サイト全体検索` / `作業ルート診断` / `全ルール`
- Freshness: `Release baseline v1.22.0 · 2026-09-07 · Current mainには未Release変更あり` と本文同期非保証の説明を表示
- Source Footer: `START_HERE.md` / `maintenance/rule-router.json` / GitHub Edit / Issue Reportへ到達可能
- `aria-current="page"`: 1件
- Keyboard: RelatedからTab操作でSource / Edit / Reportへ順に到達可能
- 390px viewport: Related links / Source Footer linksはgrid 1列へStack
- 390px viewport: body horizontal overflowなし

System Chromiumは環境Policyによりlocalhost / file Navigationがblockされるため、URL navigationそのものはBrowser test対象外。Pages #259のdeploy successとArtifact内容を別Evidenceとして確認した。

### Current Status

**作業状況: 完了**

Human Guide P1の比較調査で採用した主要項目は、P0基盤に加えてAuto TOC、Source / Edit / Report、Search Filter / Shortcut、Related / Next Steps、Freshness境界までCurrent mainへ反映済み。Current known blocking issueはない。
